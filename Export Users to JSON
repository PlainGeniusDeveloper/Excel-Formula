<!DOCTYPE html>
<html>
<head>
    <title>Extract SharePoint Group Data</title>
    <script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
</head>
<body>
    <button id="exportDataBtn" type="button">Export Data</button>
    <pre id="jsonData" style="display: none;"></pre>
    
    <script type="text/javascript">
        $(document).ready(function() {
            $("#exportDataBtn").click(function() {
                var allGroupsData = [];
                $.ajax({
                    url: _spPageContextInfo.webAbsoluteUrl + "/_api/web/sitegroups",
                    method: "GET",
                    headers: {
                        "Accept": "application/json; odata=verbose"
                    },
                    success: function(data) {
                        var groups = data.d.results;
                        var groupPromises = groups.map(function(group) {
                            return getGroupEmails(group.Id, group.Title);
                        });

                        Promise.allSettled(groupPromises).then(function() {
                            var jsonData = JSON.stringify(allGroupsData, null, 2);
                            $("#jsonData").text(jsonData).show();
                            download("sharepoint_groups.json", jsonData);
                        });
                    },
                    error: function(error) {
                        console.error("Error fetching groups: ", JSON.stringify(error));
                    }
                });

                function getGroupEmails(groupId, groupName) {
                    return $.ajax({
                        url: _spPageContextInfo.webAbsoluteUrl + "/_api/web/sitegroups/getbyid(" + groupId + ")/users",
                        method: "GET",
                        headers: {
                            "Accept": "application/json; odata=verbose"
                        },
                        success: function(data) {
                            var users = data.d.results;
                            var emailList = users.map(function(user) {
                                return user.Email;
                            });
                            allGroupsData.push({
                                "GroupTitle": groupName,
                                "Emails": emailList.join("; ")
                            });
                        },
                        error: function(error) {
                            console.error("Error fetching users for group " + groupName + ": ", JSON.stringify(error));
                        }
                    });
                }

                function download(filename, text) {
                    var element = document.createElement('a');
                    element.setAttribute('href', 'data:text/json;charset=utf-8,' + encodeURIComponent(text));
                    element.setAttribute('download', filename);
                    element.style.display = 'none';
                    document.body.appendChild(element);
                    element.click();
                    document.body.removeChild(element);
                }
            });
        });
    </script>
</body>
</html>
